# Queue-owned semantic finding repair

Repair the complete verified blocking high/critical finding group for the exact frozen candidate. Stay within the original task path admission, create exactly one logical repair commit, run only focused checks needed by the findings, and leave the worktree clean. Do not review, merge, release, push, publish, deploy, mutate controller workflow/prompt assets, or mutate external systems.

The exact candidate, canonical task/PDR revision, policy, reviewer identity, prior findings, and terminal verdict are bound by the immutable review reference embedded in this request; never query a synthetic task named `Review`. Low/medium advisories stay on the delivery and are not repair authority. Consume the code-owned mutation eligibility and exact invalidating change. Do not relaunch an unchanged deterministic reviewer or repair request. Retry only a typed transient result whose bounded safe action authorizes retry; the fenced executor rechecks live authority without status polling.

Return a typed terminal outcome: `completed`, `blocked`, or `incomplete`.
