# Managed attributable test repair

Repair only the supplied attributable failing command at the exact current task tip. Stay within frozen path authority, run only the affected focused check, create one repair commit, and leave the worktree clean. Do not mutate controller workflow/prompt assets or exercise merge/release/external authority.

Consume the code-owned mutation eligibility supplied with the lifecycle context. Do not rerun the unchanged deterministic finding: return its prior terminal evidence and required invalidating change. Retry only a typed transient result whose bounded safe action authorizes retry. The executor checks live authority internally; do not add status polling.

Return a typed terminal outcome: `completed`, `blocked`, or `incomplete`.
