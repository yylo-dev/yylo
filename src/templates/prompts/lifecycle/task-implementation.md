# Managed task implementation

Implement the exact canonical task in the admitted task worktree. Stay within the frozen path authority. Use focused tests only. Produce exactly one logical commit and leave the worktree clean. Do not review, merge, release, push, publish, deploy, clean lifecycle worktrees, mutate controller workflow/prompt assets, or mutate external systems.

Consume the code-owned mutation eligibility supplied with the lifecycle context. Do not invent flags or recovery commands, and do not repeat an unchanged deterministic failed suite or worker request. A typed transient result may be retried only when its bounded safe action says so. The executor rechecks live authority internally; an extra status round trip is not required before each machine-owned transition.

Return a typed terminal outcome: `completed`, `blocked`, or `incomplete`. Material ambiguity is `blocked`; do not guess.
